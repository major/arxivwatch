# ArXiv RSS Feed Configuration
# Comma-separated list of RSS feed URLs to monitor
# You can use shorthand names (e.g., "cs.AI") or full URLs
# Find feeds at: https://info.arxiv.org/help/rss.html
ARXIV_RSS_URLS='["cs.AI", "cs.LG"]'

# Google Gemini API Configuration
# Get your API key from: https://aistudio.google.com/app/apikey
ARXIV_GEMINI_API_KEY=your-api-key-here
ARXIV_GEMINI_MODEL=gemini-2.5-flash-lite

# Number of PDF pages to send to Gemini (default: 5)
# ARXIV_GEMINI_PDF_PAGES=5

# Gemini Prompt Configuration (choose one)
# Option 1: Provide prompt directly as environment variable
# ARXIV_GEMINI_PROMPT="Summarize this paper: {title}"

# Option 2: Read prompt from a file (recommended)
ARXIV_GEMINI_PROMPT_FILE=prompt.txt

# SMTP Email Configuration
# For Gmail: smtp.gmail.com / 587 (requires app password)
# For Outlook: smtp-mail.outlook.com / 587
ARXIV_SMTP_HOST=smtp.gmail.com
ARXIV_SMTP_PORT=587
ARXIV_SMTP_USERNAME=your-email@gmail.com
ARXIV_SMTP_PASSWORD=your-app-password-here
ARXIV_SMTP_FROM=your-email@gmail.com
ARXIV_SMTP_TO=recipient@example.com

# Storage Configuration (optional)
# ARXIV_STORAGE_FILE=notified_papers.json
